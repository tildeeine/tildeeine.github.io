const nextConfig = {
    basePath: '/<repository-name>',
    assetPrefix: '/<repository-name>/',
    trailingSlash: true,
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;